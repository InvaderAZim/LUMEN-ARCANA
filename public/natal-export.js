function addNatalExports(){const root=document.querySelector('#app'),title=root?.querySelector('.top h1')?.textContent?.trim(),wheel=root?.querySelector('svg[aria-label="Натальне колесо"]');if(title!=='Натальна карта'||!wheel||root.querySelector('#natalExportBar'))return;const panel=wheel.closest('.premium-panel');if(!panel)return;const bar=document.createElement('div');bar.id='natalExportBar';bar.className='result-actions';bar.style.marginTop='14px';bar.innerHTML='<button class="ghost" id="natalPng">Зберегти PNG</button><button class="primary" id="natalPdf">PDF / Друк</button>';panel.appendChild(bar);document.querySelector('#natalPng')?.addEventListener('click',()=>exportNatalPng(wheel));document.querySelector('#natalPdf')?.addEventListener('click',()=>window.print())}
function exportNatalPng(svg){const xml=new XMLSerializer().serializeToString(svg),blob=new Blob([xml],{type:'image/svg+xml;charset=utf-8'}),url=URL.createObjectURL(blob),img=new Image();img.onload=()=>{const canvas=document.createElement('canvas'),scale=3,box=svg.viewBox.baseVal;canvas.width=box.width*scale;canvas.height=box.height*scale;const ctx=canvas.getContext('2d');ctx.scale(scale,scale);ctx.fillStyle='#080808';ctx.fillRect(0,0,box.width,box.height);ctx.drawImage(img,0,0,box.width,box.height);URL.revokeObjectURL(url);canvas.toBlob(out=>{if(!out)return;const a=document.createElement('a');a.href=URL.createObjectURL(out);a.download=`lumen-natal-${new Date().toISOString().slice(0,10)}.png`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)},'image/png')};img.src=url}
addNatalExports();new MutationObserver(addNatalExports).observe(document.querySelector('#app')||document.body,{childList:true,subtree:true});
